# 이 저장소 안내 (Claude Code 세션용)

주식을 처음 접하는 50대 어머니를 위한 주식 비서 프로젝트입니다.

- `data/portfolio.json`: 어머니의 보유 종목(holdings)과 관심 종목(watchlist). 아침 리포트의 입력.
- `reports/YYYY-MM-DD.md`: 아침 리포트 결과물. 날짜는 한국 시간 기준.
- `src/stockmom/prompts.py` 의 `MORNING_REPORT_PROMPT`: 리포트 형식의 원본. 형식을 바꾸려면 여기를 고친다.
- `src/agentsys/`, `src/stockmom/`: API 키 방식으로 돌리는 파이썬 구현. 구독 방식(루틴)에서는 데이터 유틸만 쓴다.

## 아침 리포트를 만들 때 (루틴 세션이 따를 절차)

1. `python -m stockmom status` 를 실행해 보유/관심 종목 목록과, 가능하면 현재가·손익을 얻는다.
   이 환경은 야후 파이낸스 등 금융 사이트 접속이 막혀 있어 `price` 가 비어 있을 수 있다. 그러면 종목마다 WebSearch 로
   "<종목이름> 주가 오늘" 을 검색해 현재가와 전일 대비 등락을 찾고, 산 가격(avg_price)과 비교해 손익률을 직접 계산한다.
2. 하루 또는 한 달 변동이 큰 종목(±3% 이상)은 WebSearch 로 이유를 찾는다. 최대 3개 종목.
3. WebSearch 로 "간밤 미국 증시 마감" 과 "오늘 한국 증시 전망" 을 각각 한 번 검색해 핵심만 정리한다.
4. `src/stockmom/prompts.py` 의 MORNING_REPORT_PROMPT 에 있는 마크다운 형식 그대로 리포트를 쓴다. 표 금지, 한두 문장씩, 전문용어는 괄호로 풀이.
   "사세요/파세요" 는 쓰지 않는다. 숫자는 검색으로 확인한 것만 쓰고, 못 찾은 숫자는 "확인 안 됨" 이라고 적는다.
5. `reports/YYYY-MM-DD.md` (한국 시간 오늘 날짜) 로 저장하고 커밋한 뒤 현재 브랜치에 푸시한다.
6. 세션의 마지막 메시지에 리포트 전문을 그대로 붙인다 (이메일 알림에 실린다).
